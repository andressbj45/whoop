---
phase: 02-content-pages
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - stella-mattina/src/app/doctor-directory/[slug]/page.tsx
autonomous: true
requirements: [DOC-02, DOC-03]

must_haves:
  truths:
    - "Visiting /doctor-directory/jennifer-rouen shows her photo, name, specialty, bio, and a 'Book with Jennifer' CTA button"
    - "Visiting /doctor-directory/margaret-warren shows 'Bio coming soon.' fallback instead of crashing"
    - "All 29 doctor bio pages are statically generated at build time (generateStaticParams returns 29 slugs)"
    - "Physician JSON-LD is present in page source with name, medicalSpecialty, worksFor fields"
  artifacts:
    - path: "stella-mattina/src/app/doctor-directory/[slug]/page.tsx"
      provides: "Doctor bio page with Physician JSON-LD, photo, credentials, BookingButton"
      exports: ["generateStaticParams", "generateMetadata", "default"]
      min_lines: 70
  key_links:
    - from: "stella-mattina/src/app/doctor-directory/[slug]/page.tsx"
      to: "stella-mattina/src/lib/content/practitioners.ts"
      via: "getPractitioners() in generateStaticParams, getPractitionerBySlug(params.slug) in page"
      pattern: "getPractitionerBySlug"
    - from: "stella-mattina/src/app/doctor-directory/[slug]/page.tsx"
      to: "schema-dts Physician type"
      via: "jsonLd typed as Physician with @context and @type fields"
      pattern: "application/ld\\+json"
---

<objective>
Build the dynamic doctor bio detail page at `/doctor-directory/[slug]` — statically generating all 29 practitioner pages at build time with photo, credentials, specialties, bio, "Book with [Name]" CTA, and Physician schema.org JSON-LD.

Purpose: Doctor bio pages are the primary trust-building surface. They must be in place before the listing page (Plan 06) can link to them, and before Phase 3 homepage doctor highlights can reference them.

Output: stella-mattina/src/app/doctor-directory/[slug]/page.tsx with generateStaticParams, generateMetadata, and Physician JSON-LD.
</objective>

<execution_context>
@/Users/andressantos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andressantos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-RESEARCH.md
@stella-mattina/src/lib/content/practitioners.ts
@stella-mattina/src/lib/content/types.ts
@stella-mattina/src/components/layout/PageWrapper.tsx
@stella-mattina/src/components/common/BookingButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build doctor bio detail page with Physician JSON-LD</name>
  <files>
    stella-mattina/src/app/doctor-directory/[slug]/page.tsx
  </files>
  <action>
    Create the directory `stella-mattina/src/app/doctor-directory/[slug]/` and the page file.

    The page must:
    1. Export `generateStaticParams()` — calls getPractitioners(), maps to `{ slug: p.slug }` array (29 slugs)
    2. Export `generateMetadata({ params })` — returns title `"${p.name} | ${p.specialty} | Stella Mattina"` and description from bio (first 155 chars) or fallback `"Meet ${p.name} — ${p.specialty} at Stella Mattina in Dallas."`
    3. Export default `DoctorBioPage({ params })` that:
       - Calls getPractitionerBySlug(params.slug), calls notFound() if null
       - Builds Physician JSON-LD typed as `Physician` from schema-dts with fields: `@context: 'https://schema.org'`, `@type: 'Physician'`, `name`, `description` (bio or undefined), `medicalSpecialty` (specialty field), `image` (photo_url), `url` (`https://stellamattina.com/doctor-directory/${p.slug}`), `worksFor: { '@type': 'MedicalOrganization', name: 'Stella Mattina', url: 'https://stellamattina.com' }`
       - Injects JSON-LD via `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} />`
       - Renders inside PageWrapper

    Layout inside PageWrapper:
    ```
    <div className="mx-auto max-w-4xl px-4 py-16">
      <div className="flex flex-col sm:flex-row gap-8 items-start">
        [Image: photo_url, 240x280, rounded-xl object-cover, alt=name]
        <div>
          <h1 className="font-display text-3xl text-sm-navy">{p.name}</h1>
          <p className="mt-1 text-sm-blue font-medium">{p.specialty}</p>
          [p.locations array joined with comma, text-gray-500 text-sm]
          [bio: text-gray-700 leading-relaxed mt-6, OR fallback italic text-gray-500 "Bio coming soon."]
          <div className="mt-8">
            <BookingButton label={`Book with ${p.name.split(' ')[0]}`} size="lg" />
          </div>
        </div>
      </div>
    </div>
    ```

    Imports required:
    - `import { getPractitioners, getPractitionerBySlug } from '@/lib/content/practitioners'`
    - `import { PageWrapper } from '@/components/layout/PageWrapper'`
    - `import { BookingButton } from '@/components/common/BookingButton'`
    - `import { notFound } from 'next/navigation'`
    - `import Image from 'next/image'`
    - `import type { Metadata } from 'next'`
    - `import type { Physician } from 'schema-dts'`

    CRITICAL: next.config.ts must have remotePatterns for stellamattina.com (done in Plan 01) before this page can render practitioner photos. Do NOT use `<img>` tags — use `next/image` exclusively.

    CRITICAL: margaret-warren has no bio — the bio check is mandatory. Never render `undefined` as text.

    Page is a Server Component — NO 'use client' directive.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npm run build 2>&1 | grep -E "(doctor-directory|error|Error)" | head -20</automated>
    <manual>Visit http://localhost:3000/doctor-directory/jennifer-rouen — confirm photo visible, name "Jennifer Rouen" in h1, specialty shown, bio text present, "Book with Jennifer" button. Visit http://localhost:3000/doctor-directory/margaret-warren — confirm "Bio coming soon." appears, no crash. View page source — confirm application/ld+json script with @type Physician is present.</manual>
  </verify>
  <done>npm run build exits 0 with 29 /doctor-directory/[slug] routes prerendered. Doctor bio page renders photo, name, specialty, bio (or fallback), and BookingButton. Physician JSON-LD present in page source.</done>
</task>

</tasks>

<verification>
cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npm run build 2>&1 | grep "doctor-directory" | head -5
</verification>

<success_criteria>
- npm run build exits 0
- 29 /doctor-directory/[slug] routes appear in build output as static pages
- Page source for any doctor contains application/ld+json with "@type": "Physician"
- margaret-warren page renders without error (bio fallback shown)
- BookingButton with "Book with [FirstName]" visible on every bio page
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-02-SUMMARY.md`
</output>
