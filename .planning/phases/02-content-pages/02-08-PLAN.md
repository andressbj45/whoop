---
phase: 02-content-pages
plan: "08"
type: execute
wave: 4
depends_on: ["02-06", "02-07"]
files_modified: []
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03, SERV-01, SERV-02, SERV-03, SEOLP-01, SEOLP-02, BLOG-01, BLOG-02, BLOG-03, LOC-01, LOC-02, LOC-03, ABOUT-01, CONT-01, CONT-02, CAR-01]

must_haves:
  truths:
    - "All content pages load without errors at their correct URLs"
    - "Schema markup is visible in page source for doctor, service, blog, and location pages"
    - "Contact form has no PHI fields"
    - "The site renders correctly on mobile (375px)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 2 content pages — confirm every page type loads correctly with real SM content, correct schema markup, and proper mobile rendering.

Purpose: Phase 2 builds 100+ static pages across 8 page types. A single human review pass confirms the automated build output is correct before moving to Phase 3 homepage assembly.

Output: No files modified. Human approval to proceed to Phase 3.
</objective>

<execution_context>
@/Users/andressantos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andressantos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final build and start dev server for review</name>
  <files></files>
  <action>
    Run the final build and start the dev server for human review:

    ```bash
    cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina
    npm run build 2>&1 | tail -20
    npm run dev
    ```

    Confirm build exits 0 before reporting ready for review. If build fails, report the error — do not proceed to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npm run build 2>&1 | grep -E "^(Route|Error|error)" | head -30</automated>
  </verify>
  <done>npm run build exits 0, dev server running at http://localhost:3000.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 2 content pages</name>
  <action>Human reviews all Phase 2 page types in browser at http://localhost:3000. No code changes — verification only.</action>
  <what-built>
    All Phase 2 content pages — 29 doctor bio pages, 10 service pages, 61 blog posts, 7 location detail pages, doctor directory listing, blog index, locations listing, About Us, Contact, and Careers.
  </what-built>
  <how-to-verify>
    Visit each URL below and confirm the expected content. Dev server: http://localhost:3000

    **Doctor pages (spot check 3):**
    1. http://localhost:3000/doctor-directory — grid of 29 doctors with photos
    2. http://localhost:3000/doctor-directory/jennifer-rouen — photo, bio, specialty, "Book with Jennifer" button
    3. http://localhost:3000/doctor-directory/margaret-warren — "Bio coming soon." shown, no crash
    View source → confirm `"@type": "Physician"` in page source

    **Service pages (spot check 2):**
    4. http://localhost:3000/gynecology — full content, FAQ section with 3+ questions visible
    5. http://localhost:3000/biote-hormone-therapy — content and FAQ section
    View source → confirm `"@type": "FAQPage"` with non-empty `mainEntity` array
    6. http://localhost:3000/providers-bio — should redirect to /doctor-directory

    **Blog (spot check 2):**
    7. http://localhost:3000/blog — all posts listed, category filter buttons clickable
    8. http://localhost:3000/blog/[any-slug] — readable paragraphs, no tag cloud at bottom, author byline visible
    View source → confirm `"@type": "Article"` in page source

    **Locations (spot check 2):**
    9. http://localhost:3000/find-our-locations — 7 location cards with addresses
    10. http://localhost:3000/find-our-locations/gynecologist-dallas-bishop-arts — address, phone, "Get Directions" link, providers list
    View source → confirm `"@type": "MedicalClinic"` with address in page source

    **Static pages:**
    11. http://localhost:3000/about-us — clinic story content, BookingButton
    12. http://localhost:3000/contact-us — phone numbers, 3-field form ONLY (name/email/message — no DOB, no diagnosis)
    13. http://localhost:3000/careers — culture content, mailto: application link

    **Mobile check:**
    14. Open DevTools → toggle device toolbar → 375px width → visit /doctor-directory — no horizontal scroll, cards stack vertically
  </how-to-verify>
  <verify>
    <manual>All 14 checks above pass in browser.</manual>
  </verify>
  <done>Human confirms all page types render correctly with real SM content and schema markup.</done>
  <resume-signal>Type "approved" to proceed to Phase 3 planning, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human approval received via resume-signal.
</verification>

<success_criteria>
- All 13 spot-check URLs load without errors
- Schema markup present in source for doctor, service, blog, and location pages
- Contact form has exactly 3 fields (name, email, message) — no PHI fields
- Mobile layout correct at 375px
- Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-08-SUMMARY.md`
</output>
