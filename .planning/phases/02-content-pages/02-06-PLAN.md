---
phase: 02-content-pages
plan: "06"
type: execute
wave: 3
depends_on: ["02-02", "02-04"]
files_modified:
  - stella-mattina/src/app/doctor-directory/page.tsx
  - stella-mattina/src/app/blog/page.tsx
  - stella-mattina/src/components/blog/BlogFilter.tsx
autonomous: true
requirements: [DOC-01, BLOG-01]

must_haves:
  truths:
    - "Visiting /doctor-directory shows a grid of all 29 practitioners with photo, name, specialty, and a link to their bio page"
    - "Visiting /blog shows all 61 posts with title, author, date, and category badge"
    - "Blog category filter buttons appear at the top of /blog and clicking one filters the visible posts without a page reload"
    - "All doctor listing cards link to /doctor-directory/[slug] pages (built in Plan 02)"
  artifacts:
    - path: "stella-mattina/src/app/doctor-directory/page.tsx"
      provides: "Doctor directory listing page with 29 practitioner cards"
      exports: ["metadata", "default"]
    - path: "stella-mattina/src/app/blog/page.tsx"
      provides: "Blog index page — Server Component parent for BlogFilter client island"
      exports: ["metadata", "default"]
    - path: "stella-mattina/src/components/blog/BlogFilter.tsx"
      provides: "Client component receiving all posts and categories, handles category filter state"
      exports: ["BlogFilter"]
  key_links:
    - from: "stella-mattina/src/app/doctor-directory/page.tsx"
      to: "stella-mattina/src/lib/content/practitioners.ts"
      via: "getPractitioners() called in Server Component to get all 29 practitioners"
      pattern: "getPractitioners"
    - from: "stella-mattina/src/app/blog/page.tsx"
      to: "stella-mattina/src/components/blog/BlogFilter.tsx"
      via: "passes posts and categories as props to BlogFilter client island"
      pattern: "BlogFilter"
    - from: "stella-mattina/src/components/blog/BlogFilter.tsx"
      to: "post data"
      via: "useState for active category, filters posts array prop, renders filtered list"
      pattern: "useState"
---

<objective>
Build the doctor directory listing page and the blog index page (with client-side category filter). Both pages depend on their detail pages existing first (Plans 02 and 04) so links can be verified.

Purpose: Listing pages aggregate and surface the detail pages built in Wave 2. The doctor directory is the primary navigation entry point for patient trust. The blog index with category filtering serves content discovery.

Output: /doctor-directory/page.tsx (Server Component, 29 practitioner cards), /blog/page.tsx (Server Component parent), /src/components/blog/BlogFilter.tsx (client island with category filter).
</objective>

<execution_context>
@/Users/andressantos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andressantos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-RESEARCH.md
@stella-mattina/src/lib/content/practitioners.ts
@stella-mattina/src/lib/content/blog.ts
@stella-mattina/src/lib/content/types.ts
@stella-mattina/src/components/layout/PageWrapper.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build doctor directory listing page</name>
  <files>
    stella-mattina/src/app/doctor-directory/page.tsx
  </files>
  <action>
    Create `stella-mattina/src/app/doctor-directory/page.tsx`. This is a Server Component that renders all 29 practitioners in a card grid.

    ```typescript
    import { getPractitioners } from '@/lib/content/practitioners'
    import { PageWrapper } from '@/components/layout/PageWrapper'
    import { BookingButton } from '@/components/common/BookingButton'
    import Image from 'next/image'
    import type { Metadata } from 'next'

    export const metadata: Metadata = {
      title: 'Our Doctors & Providers | Stella Mattina',
      description: 'Meet the Stella Mattina team — experienced OB-GYNs, maternal-fetal medicine specialists, and primary care physicians serving Dallas and the DFW area.',
    }

    export default function DoctorDirectoryPage() {
      const practitioners = getPractitioners()

      return (
        <PageWrapper>
          <div className="mx-auto max-w-6xl px-4 py-16">
            <h1 className="font-display text-4xl text-sm-navy mb-4">Meet Our Providers</h1>
            <p className="text-gray-600 mb-12 max-w-2xl">
              Our team of board-certified physicians and specialists is dedicated to women's health, obstetrics, and primary care across the Dallas-Fort Worth area.
            </p>
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
              {practitioners.map((p) => (
                <a
                  key={p.slug}
                  href={`/doctor-directory/${p.slug}`}
                  className="group block border border-gray-200 rounded-xl overflow-hidden hover:border-sm-blue hover:shadow-md transition-all"
                >
                  <div className="relative aspect-[3/2] bg-gray-50">
                    <Image
                      src={p.photo_url}
                      alt={p.name}
                      fill
                      className="object-cover object-top"
                    />
                  </div>
                  <div className="p-4">
                    <h2 className="font-display text-lg text-sm-navy group-hover:text-sm-blue transition-colors">{p.name}</h2>
                    <p className="text-sm text-gray-500 mt-1">{p.specialty}</p>
                    <p className="text-xs text-gray-400 mt-1">{p.locations.join(', ')}</p>
                  </div>
                </a>
              ))}
            </div>
            <div className="mt-12 text-center">
              <BookingButton label="Book an Appointment" size="lg" />
            </div>
          </div>
        </PageWrapper>
      )
    }
    ```

    CRITICAL: Use `next/image` with `fill` for the card photos — the remotePatterns in next.config.ts (Plan 01) enables stellamattina.com photos. Set `object-top` to show faces rather than cropping to torso. Use `aspect-[3/2]` with `relative` on the container.

    Page is a Server Component — NO 'use client' directive.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npm run build 2>&1 | grep "doctor-directory" | grep -v "slug" | head -5</automated>
    <manual>Visit http://localhost:3000/doctor-directory — confirm grid of practitioner cards with photos, names, and specialties. Confirm clicking a card navigates to /doctor-directory/[slug].</manual>
  </verify>
  <done>npm run build exits 0, /doctor-directory renders a card grid of all 29 practitioners with photos and links to bio pages.</done>
</task>

<task type="auto">
  <name>Task 2: Build blog index page with client-side category filter</name>
  <files>
    stella-mattina/src/app/blog/page.tsx
    stella-mattina/src/components/blog/BlogFilter.tsx
  </files>
  <action>
    **stella-mattina/src/components/blog/BlogFilter.tsx:** Create a 'use client' component that receives all posts and categories as props and handles client-side filtering. Uses useState for the active category:

    ```typescript
    'use client'
    import { useState } from 'react'
    import type { BlogPost } from '@/lib/content/types'

    interface Props {
      posts: BlogPost[]
      categories: string[]
    }

    export function BlogFilter({ posts, categories }: Props) {
      const [active, setActive] = useState<string | null>(null)
      const filtered = active
        ? posts.filter((p) => {
            const cats = Array.isArray(p.categories)
              ? p.categories.map((c) => typeof c === 'string' ? c : Object.values(c as Record<string, string>).join(' '))
              : []
            return cats.some((c) => c.toLowerCase() === active.toLowerCase())
          })
        : posts

      return (
        <>
          {/* Category filter */}
          <div className="flex flex-wrap gap-2 mb-8">
            <button
              onClick={() => setActive(null)}
              className={`px-4 py-1.5 rounded-full text-sm font-medium transition-colors ${
                !active ? 'bg-sm-navy text-white' : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
              }`}
            >
              All Posts
            </button>
            {categories.map((cat) => (
              <button
                key={cat}
                onClick={() => setActive(cat === active ? null : cat)}
                className={`px-4 py-1.5 rounded-full text-sm font-medium transition-colors ${
                  active === cat ? 'bg-sm-blue text-white' : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
                }`}
              >
                {cat}
              </button>
            ))}
          </div>

          {/* Post list */}
          <div className="space-y-6">
            {filtered.map((post) => (
              <a
                key={post.slug}
                href={`/blog/${post.slug}`}
                className="block border border-gray-200 rounded-xl p-6 hover:border-sm-blue hover:shadow-sm transition-all"
              >
                <div className="flex items-start justify-between gap-4">
                  <div>
                    <h2 className="font-display text-xl text-sm-navy hover:text-sm-blue transition-colors">
                      {post.heading}
                    </h2>
                    <div className="flex items-center gap-3 mt-2 text-sm text-gray-500">
                      <span>{post.author}</span>
                      <span>·</span>
                      <time dateTime={post.published_date}>
                        {new Date(post.published_date).toLocaleDateString('en-US', {
                          year: 'numeric', month: 'short', day: 'numeric',
                        })}
                      </time>
                    </div>
                  </div>
                  {Array.isArray(post.categories) && typeof post.categories[0] === 'string' && (
                    <span className="shrink-0 px-3 py-1 rounded-full text-xs font-medium bg-blue-50 text-sm-blue">
                      {String(post.categories[0])}
                    </span>
                  )}
                </div>
                {post.meta_description && (
                  <p className="mt-3 text-gray-600 text-sm line-clamp-2">{post.meta_description}</p>
                )}
              </a>
            ))}
          </div>
          <p className="mt-6 text-sm text-gray-400 text-right">{filtered.length} post{filtered.length !== 1 ? 's' : ''}</p>
        </>
      )
    }
    ```

    **stella-mattina/src/app/blog/page.tsx:** Create the Server Component parent that fetches data and passes it to BlogFilter:

    ```typescript
    import { getBlogPosts, getBlogCategories } from '@/lib/content/blog'
    import { PageWrapper } from '@/components/layout/PageWrapper'
    import { BlogFilter } from '@/components/blog/BlogFilter'
    import type { Metadata } from 'next'

    export const metadata: Metadata = {
      title: 'Women\'s Health Blog | Stella Mattina',
      description: "Expert insights on women's health, obstetrics, gynecology, and primary care from the Stella Mattina team.",
    }

    export default function BlogIndexPage() {
      const posts = getBlogPosts()
      const categories = getBlogCategories()

      return (
        <PageWrapper>
          <div className="mx-auto max-w-4xl px-4 py-16">
            <h1 className="font-display text-4xl text-sm-navy mb-4">Women's Health Blog</h1>
            <p className="text-gray-600 mb-10 max-w-2xl">
              Expert guidance on pregnancy, gynecology, hormone health, and primary care from our team of board-certified physicians.
            </p>
            <BlogFilter posts={posts} categories={categories} />
          </div>
        </PageWrapper>
      )
    }
    ```

    CRITICAL: BlogFilter is a client island — it has 'use client'. The parent blog/page.tsx must NOT have 'use client' — it fetches data server-side and passes it as props. This is the correct client island pattern established in Phase 1.

    CRITICAL: Pass serializable data only (plain BlogPost objects from getBlogPosts()) — no functions, no class instances passed as props to the client component.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npm run build 2>&1 | grep -E "(/blog$|/blog )" | head -5</automated>
    <manual>Visit http://localhost:3000/blog — confirm h1, category filter buttons appear, and all posts are listed. Click a category button — confirm list filters without page reload. Confirm post cards link to /blog/[slug].</manual>
  </verify>
  <done>npm run build exits 0. /blog renders all 61 posts with category filter buttons. Clicking a category filters the list client-side without reload. Post cards link to /blog/[slug] detail pages.</done>
</task>

</tasks>

<verification>
cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npm run build 2>&1 | tail -15
</verification>

<success_criteria>
- npm run build exits 0
- /doctor-directory renders 29 practitioner cards with photos and bio page links
- /blog renders 61 posts with category filter
- BlogFilter client island filters posts without page reload
- No 'use client' in blog/page.tsx or doctor-directory/page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-06-SUMMARY.md`
</output>
