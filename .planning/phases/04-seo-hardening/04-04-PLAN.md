---
phase: 04-seo-hardening
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified: []
autonomous: false
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "sitemap.xml is reachable and contains all page categories"
    - "robots.txt is reachable and grants AI crawlers"
    - "Every page type has canonical, OG image, and (for non-home pages) BreadcrumbList JSON-LD"
  artifacts:
    - path: "stella-mattina/src/app/sitemap.ts"
      provides: "Auto-served sitemap.xml"
      exports: ["default"]
    - path: "stella-mattina/src/app/robots.ts"
      provides: "Auto-served robots.txt"
      exports: ["default"]
  key_links:
    - from: "https://stellamattina.com/sitemap.xml"
      to: "stella-mattina/src/app/sitemap.ts"
      via: "Next.js built-in file convention auto-serving"
      pattern: "MetadataRoute.Sitemap"
---

<objective>
Human verification of all Phase 4 SEO hardening work across a running dev server. Confirms that sitemap, robots.txt, canonical tags, OG metadata, and BreadcrumbList JSON-LD are all present and correct in the actual browser-rendered output.

Purpose: TypeScript checks confirm the code compiles but cannot confirm what the browser actually receives. This checkpoint validates end-to-end correctness before the phase is marked complete.
Output: Confirmed or flagged issues list.
</objective>

<execution_context>
@/Users/andressantos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andressantos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build verification</name>
  <files></files>
  <action>
Run a production build to catch any metadata warnings or errors that only appear at build time (not just TypeScript type errors):

```bash
cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npm run build 2>&1
```

Scan the output for:
1. Any warnings containing "metadataBase" — would indicate metadataBase is missing or not resolving
2. Any TypeScript errors that tsc --noEmit may have missed (rare but possible with Next.js-specific types)
3. The page count in the build output — should show all expected route groups

If the build succeeds, start the dev server for the human verification checkpoint that follows.

If build fails, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Build completes with no errors. No "metadataBase" warnings in build output. Page count in build output includes all expected routes (doctors, blog posts, locations, services, static pages).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all SEO hardening outputs</name>
  <action>
Start the dev server (`cd stella-mattina && npm run dev`) and verify each SEO requirement in browser. See how-to-verify for exact steps.
  </action>
  <what-built>
Phase 4 SEO hardening: sitemap.xml, robots.txt, canonical URLs, Open Graph metadata, and BreadcrumbList JSON-LD across all 119+ pages (19 static + 97 dynamic).
  </what-built>
  <how-to-verify>
Start the dev server: `cd stella-mattina && npm run dev`

Then verify each item:

**1. Sitemap (SEO-01)**
- Visit http://localhost:3000/sitemap.xml
- Confirm: XML loads, contains URLs for homepage, doctor pages, blog posts, location pages, service pages
- Check: providers-bio URL does NOT appear in the sitemap

**2. Robots.txt (SEO-02)**
- Visit http://localhost:3000/robots.txt
- Confirm: Shows rules for ChatGPT-User, GPTBot, OAI-SearchBot, PerplexityBot, Perplexity-User, Googlebot, Google-Extended — all with Allow: /
- Confirm: Sitemap URL line is present at the bottom

**3. Canonical URL (SEO-05)**
- Visit http://localhost:3000/about-us and View Page Source (Cmd+U)
- Find: `<link rel="canonical" href="https://stellamattina.com/about-us" />`
- Visit http://localhost:3000/doctor-directory/[any-doctor-slug] and View Page Source
- Find: `<link rel="canonical" href="https://stellamattina.com/doctor-directory/[slug]" />`

**4. Open Graph metadata (SEO-03)**
- In the about-us page source, find: `<meta property="og:image"` with an image URL
- In the blog post page source, find: `<meta property="og:type" content="article" />`
- In any page source, confirm: image tag always appears (not being dropped by shallow merge)

**5. BreadcrumbList JSON-LD (SEO-04)**
- In the about-us page source, find: `<script type="application/ld+json">` containing `"@type":"BreadcrumbList"`
- In the doctor page source, find the same — with 3 items: Home, Doctor Directory, doctor name
- In the homepage source (http://localhost:3000), confirm: NO BreadcrumbList JSON-LD present (homepage must not have one)
  </how-to-verify>
  <verify>
    <automated>MISSING — This is a human checkpoint requiring browser inspection of rendered HTML output</automated>
    <manual>See how-to-verify section for the 5 browser checks to perform</manual>
  </verify>
  <done>Human confirms all 5 SEO items pass. Resume signal: "approved"</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found so they can be fixed before the phase is marked complete.</resume-signal>
</task>

</tasks>

<verification>
All five SEO-hardening requirements verified in browser:
- SEO-01: /sitemap.xml accessible with all page URL categories
- SEO-02: /robots.txt grants all specified AI crawlers
- SEO-03: OG title, description, image present on all pages
- SEO-04: BreadcrumbList JSON-LD on all non-home pages
- SEO-05: Canonical link tag on all pages pointing to own URL
</verification>

<success_criteria>
- Production build completes with no errors or metadataBase warnings
- All 5 SEO checklist items verified in browser
- Human approves all items with "approved" signal
- Phase 4 marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-hardening/04-04-SUMMARY.md`
</output>
