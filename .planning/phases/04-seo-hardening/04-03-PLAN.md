---
phase: 04-seo-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - stella-mattina/src/app/doctor-directory/[slug]/page.tsx
  - stella-mattina/src/app/blog/[slug]/page.tsx
  - stella-mattina/src/app/find-our-locations/[slug]/page.tsx
autonomous: true
requirements:
  - SEO-03
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "Every doctor bio page has a canonical URL and OG metadata in its generateMetadata return value"
    - "Every blog post page has a canonical URL and OG metadata in its generateMetadata return value"
    - "Every location detail page has a canonical URL and OG metadata in its generateMetadata return value"
    - "All three dynamic route page components render BreadcrumbJsonLd with correct path-specific items arrays"
  artifacts:
    - path: "stella-mattina/src/app/doctor-directory/[slug]/page.tsx"
      provides: "Doctor bio page with canonical + OG + breadcrumb"
      contains: "alternates"
    - path: "stella-mattina/src/app/blog/[slug]/page.tsx"
      provides: "Blog post page with canonical + OG + breadcrumb"
      contains: "alternates"
    - path: "stella-mattina/src/app/find-our-locations/[slug]/page.tsx"
      provides: "Location page with canonical + OG + breadcrumb"
      contains: "alternates"
  key_links:
    - from: "stella-mattina/src/app/doctor-directory/[slug]/page.tsx"
      to: "stella-mattina/src/components/seo/BreadcrumbJsonLd.tsx"
      via: "import and JSX render passing slug-specific items"
      pattern: "BreadcrumbJsonLd"
    - from: "stella-mattina/src/app/blog/[slug]/page.tsx"
      to: "stella-mattina/src/components/seo/BreadcrumbJsonLd.tsx"
      via: "import and JSX render passing post title in items"
      pattern: "BreadcrumbJsonLd"
    - from: "stella-mattina/src/app/find-our-locations/[slug]/page.tsx"
      to: "stella-mattina/src/components/seo/BreadcrumbJsonLd.tsx"
      via: "import and JSX render passing location name in items"
      pattern: "BreadcrumbJsonLd"
---

<objective>
Update the three dynamic route pages (doctor-directory/[slug], blog/[slug], find-our-locations/[slug]) to add canonical URLs, Open Graph metadata, and BreadcrumbJsonLd JSON-LD. These pages use generateMetadata (not static export const metadata) and require slug-specific values in each field.

Purpose: 97 dynamic pages (29 doctors + 61 blog posts + 7 locations) are currently missing canonical tags, OG metadata, and breadcrumb JSON-LD — completing these closes the last gap in SEO-03, SEO-04, and SEO-05 requirements.
Output: Three updated dynamic route pages with complete per-page SEO metadata and BreadcrumbJsonLd.
</objective>

<execution_context>
@/Users/andressantos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andressantos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-hardening/04-RESEARCH.md
@.planning/phases/04-seo-hardening/04-01-SUMMARY.md
@.planning/phases/04-seo-hardening/04-02-SUMMARY.md

@stella-mattina/src/lib/seo/og.ts
@stella-mattina/src/components/seo/BreadcrumbJsonLd.tsx
@stella-mattina/src/app/doctor-directory/[slug]/page.tsx
@stella-mattina/src/app/blog/[slug]/page.tsx
@stella-mattina/src/app/find-our-locations/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical, OG, and breadcrumb to doctor and location dynamic pages</name>
  <files>stella-mattina/src/app/doctor-directory/[slug]/page.tsx
stella-mattina/src/app/find-our-locations/[slug]/page.tsx</files>
  <action>
For `stella-mattina/src/app/doctor-directory/[slug]/page.tsx`:

READ the file first. It has an async `generateMetadata` function returning `Promise<Metadata>`. Update the return value to ADD:
1. `alternates: { canonical: \`/doctor-directory/\${slug}\` }`
2. `openGraph: { title: ..., description: ..., url: \`/doctor-directory/\${slug}\`, type: 'website', images: [defaultOgImage] }`

Import `defaultOgImage` from `'@/lib/seo/og'`.
Import `{ BreadcrumbJsonLd }` from `'@/components/seo/BreadcrumbJsonLd'`.

For the description in generateMetadata: use `p.bio ? p.bio.slice(0, 155) : \`Meet \${p.name} — \${p.specialty} at Stella Mattina in Dallas.\`` (bio slice for natural description, fallback for doctors without bio).

In the page component (default export function), add `<BreadcrumbJsonLd>` as the first child inside the root container:
```typescript
<BreadcrumbJsonLd
  items={[
    { name: 'Home', url: 'https://stellamattina.com' },
    { name: 'Doctor Directory', url: 'https://stellamattina.com/doctor-directory' },
    { name: practitioner.name, url: `https://stellamattina.com/doctor-directory/${practitioner.slug}` },
  ]}
/>
```
Use whatever variable name the existing page component uses for the practitioner data (read the file to confirm).

ALSO handle the `not found` case in generateMetadata — when practitioner not found, return `{ title: 'Doctor Not Found | Stella Mattina' }` with no canonical or OG (preserve existing not-found behavior).

---

For `stella-mattina/src/app/find-our-locations/[slug]/page.tsx`:

READ the file first. Apply same pattern:
1. Import `defaultOgImage` from `'@/lib/seo/og'`
2. Import `{ BreadcrumbJsonLd }` from `'@/components/seo/BreadcrumbJsonLd'`
3. In `generateMetadata` return: add `alternates: { canonical: \`/find-our-locations/\${slug}\` }` and `openGraph: { title: ..., description: ..., url: \`/find-our-locations/\${slug}\`, type: 'website', images: [defaultOgImage] }`
4. In page component: add `<BreadcrumbJsonLd items={[{ name: 'Home', url: 'https://stellamattina.com' }, { name: 'Locations', url: 'https://stellamattina.com/find-our-locations' }, { name: location.name, url: \`https://stellamattina.com/find-our-locations/\${location.slug}\` }]} />`

Use the actual variable names from the existing file for the location data object.

Do NOT introduce `export const metadata` — these files already use `generateMetadata`. Having both in the same file causes a Next.js build error.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Start dev server. View source on any doctor page (e.g., /doctor-directory/[any-slug]) — confirm canonical link tag, og:image meta tag, and BreadcrumbList JSON-LD script all present. Same check on any location page.</manual>
  </verify>
  <done>TypeScript passes. Doctor and location dynamic pages return alternates.canonical and openGraph with images from generateMetadata. Page components render BreadcrumbJsonLd with 3-item breadcrumb (Home > section > page).</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical, OG, and breadcrumb to blog post dynamic page</name>
  <files>stella-mattina/src/app/blog/[slug]/page.tsx</files>
  <action>
READ `stella-mattina/src/app/blog/[slug]/page.tsx` first. It has an async `generateMetadata` function.

Import `defaultOgImage` from `'@/lib/seo/og'`.
Import `{ BreadcrumbJsonLd }` from `'@/components/seo/BreadcrumbJsonLd'`.

In `generateMetadata` return value, ADD:
1. `alternates: { canonical: \`/blog/\${slug}\` }`
2. `openGraph: { title: ..., description: ..., url: \`/blog/\${slug}\`, type: 'article', images: [defaultOgImage] }`

Note: Blog posts should use `type: 'article'` (not 'website') in openGraph — this is the correct OG type for blog content and enables richer social sharing previews.

For the OG title and description: reuse the same values computed in generateMetadata for the page `title` and `description` fields. Do not create separate descriptions.

In the page component (default export function), add `<BreadcrumbJsonLd>` as the first child in the root container:
```typescript
<BreadcrumbJsonLd
  items={[
    { name: 'Home', url: 'https://stellamattina.com' },
    { name: 'Blog', url: 'https://stellamattina.com/blog' },
    { name: post.title, url: `https://stellamattina.com/blog/${post.slug}` },
  ]}
/>
```
Use the actual variable name for the blog post object from the existing file (read the file to confirm).

Handle the not-found case in generateMetadata: when post not found, return `{ title: 'Post Not Found | Stella Mattina' }` with no canonical or OG (consistent with how doctor-directory handles not-found).

Do NOT use export const metadata — the file already uses generateMetadata. Using both causes a build error.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Start dev server. View source on any blog post page — confirm: canonical link pointing to https://stellamattina.com/blog/[slug], og:image meta tag, og:type = article, and BreadcrumbList script tag with 3 items (Home, Blog, post title).</manual>
  </verify>
  <done>TypeScript passes. Blog post dynamic page returns alternates.canonical and openGraph with type:'article' and images from generateMetadata. Page component renders 3-item BreadcrumbJsonLd (Home > Blog > post title).</done>
</task>

</tasks>

<verification>
Run TypeScript check: `cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npx tsc --noEmit`

Start dev server and verify:
1. Doctor page source: `<link rel="canonical" href="https://stellamattina.com/doctor-directory/[slug]">`
2. Doctor page source: `<meta property="og:image">` tag present
3. Doctor page source: `BreadcrumbList` JSON-LD with 3 items
4. Blog post source: same three checks, plus `og:type = article`
5. Location page source: same three checks
</verification>

<success_criteria>
- All three dynamic route files (doctor, blog, location) pass TypeScript with no errors
- generateMetadata in each file returns alternates.canonical and openGraph with images
- Blog posts use type: 'article' in openGraph
- All three page components render BreadcrumbJsonLd with 3-level breadcrumb
- No 'export const metadata' added to any of these files (would cause build error)
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-hardening/04-03-SUMMARY.md`
</output>
