---
phase: 04-seo-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - stella-mattina/src/app/sitemap.ts
  - stella-mattina/src/app/robots.ts
  - stella-mattina/src/app/layout.tsx
  - stella-mattina/src/lib/seo/og.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02

must_haves:
  truths:
    - "GET /sitemap.xml returns 200 with Content-Type: application/xml and lists all known page URLs"
    - "GET /robots.txt returns 200 and explicitly grants ChatGPT-User, GPTBot, OAI-SearchBot, PerplexityBot, Perplexity-User, Googlebot, and Google-Extended"
    - "Root layout metadata has metadataBase set to https://stellamattina.com so all relative canonical and OG paths resolve correctly"
    - "A shared defaultOgImage constant exists and is importable from @/lib/seo/og for use across all page metadata updates"
  artifacts:
    - path: "stella-mattina/src/app/sitemap.ts"
      provides: "MetadataRoute.Sitemap covering all routes"
      exports: ["default"]
      contains: "MetadataRoute.Sitemap"
    - path: "stella-mattina/src/app/robots.ts"
      provides: "MetadataRoute.Robots with AI crawler rules"
      exports: ["default"]
      contains: "ChatGPT-User"
    - path: "stella-mattina/src/app/layout.tsx"
      provides: "Root layout with metadataBase"
      contains: "metadataBase"
    - path: "stella-mattina/src/lib/seo/og.ts"
      provides: "Shared OG image constant"
      exports: ["defaultOgImage"]
  key_links:
    - from: "stella-mattina/src/app/sitemap.ts"
      to: "stella-mattina/src/lib/content/*"
      via: "getPractitioners, getBlogPosts, getLocations, getServices imports"
      pattern: "from '@/lib/content"
    - from: "stella-mattina/src/app/layout.tsx"
      to: "https://stellamattina.com"
      via: "metadataBase: new URL('https://stellamattina.com')"
      pattern: "metadataBase"
---

<objective>
Create sitemap.ts and robots.ts using Next.js built-in file conventions, add metadataBase to root layout, and create shared OG image constant — the foundation that all subsequent SEO metadata updates in Plans 02 and 03 depend on.

Purpose: Sitemap and robots.txt are the crawlability layer — search engines and AI crawlers cannot reliably discover pages without them. metadataBase is required before any relative canonical or OG paths can resolve correctly.
Output: /sitemap.xml serving all 119+ page URLs, /robots.txt allowing all major crawlers including AI bots, root layout with metadataBase, and shared og.ts constant.
</objective>

<execution_context>
@/Users/andressantos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andressantos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-hardening/04-RESEARCH.md

@stella-mattina/src/app/layout.tsx
@stella-mattina/src/lib/content/practitioners.ts
@stella-mattina/src/lib/content/blog.ts
@stella-mattina/src/lib/content/locations.ts
@stella-mattina/src/lib/content/services.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sitemap.ts and robots.ts</name>
  <files>stella-mattina/src/app/sitemap.ts
stella-mattina/src/app/robots.ts</files>
  <action>
Create `stella-mattina/src/app/sitemap.ts` using `MetadataRoute.Sitemap` return type. Import `getPractitioners`, `getBlogPosts`, `getLocations`, `getServices` from the existing content layer. Define `BASE_URL = 'https://stellamattina.com'` as a module-level const. Build four dynamic URL arrays:

- `doctorUrls`: `getPractitioners().map(p => ({ url: \`\${BASE_URL}/doctor-directory/\${p.slug}\`, lastModified: new Date(), changeFrequency: 'monthly' as const, priority: 0.7 }))`
- `blogUrls`: `getBlogPosts().map(post => ({ url: \`\${BASE_URL}/blog/\${post.slug}\`, lastModified: new Date(post.published_date), changeFrequency: 'never' as const, priority: 0.5 }))`
- `locationUrls`: `getLocations().map(loc => ({ url: \`\${BASE_URL}/find-our-locations/\${loc.slug}\`, lastModified: new Date(), changeFrequency: 'monthly' as const, priority: 0.8 }))`
- `serviceUrls`: `getServices().filter(s => s.slug !== 'providers-bio').map(s => ({ url: \`\${BASE_URL}/\${s.slug}\`, lastModified: new Date(), changeFrequency: 'monthly' as const, priority: 0.8 }))`

CRITICAL: Filter out `providers-bio` from serviceUrls — that route is a server-side redirect to /doctor-directory, not a real page. Including it would create a redirect in sitemap.

Build `staticUrls: MetadataRoute.Sitemap` array with all static pages including the homepage (priority: 1.0, changeFrequency: 'weekly'), listing pages (priority: 0.9-0.8), and all 8 SEO landing pages (gynecologist-dallas, hormone-pellet-therapy-dallas, prenatal-care-near-me, primary-care-physician-dallas, maternal-fetal-medicine, ginecologo-dallas, biote-hormone-therapy, womens-health) at priority 0.7-0.9. Static pages: homepage, doctor-directory, blog, find-our-locations, services, about-us, contact-us, careers, patient-information.

Return `[...staticUrls, ...doctorUrls, ...blogUrls, ...locationUrls, ...serviceUrls]`.

---

Create `stella-mattina/src/app/robots.ts` returning `MetadataRoute.Robots`. Use per-agent rules array with explicit allow '/' for: `*` (catch-all), `['ChatGPT-User', 'GPTBot', 'OAI-SearchBot']`, `['PerplexityBot', 'Perplexity-User']`, `['Googlebot', 'Google-Extended']`. Set `sitemap: 'https://stellamattina.com/sitemap.xml'`.

Do NOT use a static `public/robots.txt` file — the .ts convention allows programmatic composition and TypeScript type safety. Do NOT use the `next-sitemap` package — the built-in convention is zero-dependency and runs at build time correctly.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Start dev server and confirm: curl http://localhost:3000/sitemap.xml returns XML with URLs, curl http://localhost:3000/robots.txt shows AI crawler rules and Sitemap line</manual>
  </verify>
  <done>sitemap.ts and robots.ts both pass TypeScript check with no errors. /sitemap.xml serves all page categories (static, doctors, blog, locations, services). /robots.txt explicitly mentions ChatGPT-User, PerplexityBot, and Googlebot with Allow: /</done>
</task>

<task type="auto">
  <name>Task 2: Add metadataBase to root layout and create shared OG constant</name>
  <files>stella-mattina/src/app/layout.tsx
stella-mattina/src/lib/seo/og.ts</files>
  <action>
Create `stella-mattina/src/lib/seo/og.ts` — a shared constant file. Export a single named constant:

```typescript
export const defaultOgImage = {
  url: '/images/hero-doctor-consultation.jpg',
  width: 1200,
  height: 800,
  alt: "Stella Mattina Women's Health — OBGYN Dallas",
}
```

Note: Using `hero-doctor-consultation.jpg` as the fallback OG image because it is the only wide-format image available in `public/images/`. It is not the ideal 1200×630 crop but is functional. A proper OG crop is a v2 task.

---

In `stella-mattina/src/app/layout.tsx`, update the existing `export const metadata: Metadata` object. READ the file first to see the existing fields. ADD the following fields without removing any existing fields:

1. `metadataBase: new URL('https://stellamattina.com')` — add as the FIRST field in the metadata object. This is required before any page-level relative canonical or OG paths will resolve correctly.
2. `openGraph` block at the root layout level:
```typescript
openGraph: {
  siteName: 'Stella Mattina',
  locale: 'en_US',
  type: 'website',
  images: [defaultOgImage],
},
```

Import `defaultOgImage` from `@/lib/seo/og` at the top of layout.tsx.

IMPORTANT: Do NOT change the existing `title.template`, `title.default`, or `description` fields — preserve them exactly as they are. Only ADD `metadataBase` and `openGraph`.

IMPORTANT: Next.js metadata merging is SHALLOW — child pages that set any `openGraph` field must re-include `images: [defaultOgImage]` or the layout OG image will be dropped. Plans 02 and 03 handle this for each page.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm stella-mattina/src/app/layout.tsx contains 'metadataBase' and stella-mattina/src/lib/seo/og.ts exports defaultOgImage</manual>
  </verify>
  <done>TypeScript passes with no errors. layout.tsx exports metadata with metadataBase set to new URL('https://stellamattina.com'). og.ts exports defaultOgImage constant with url, width, height, and alt fields.</done>
</task>

</tasks>

<verification>
Run TypeScript check: `cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npx tsc --noEmit`

Start dev server and verify:
1. `curl http://localhost:3000/sitemap.xml` — returns XML with `<url>` entries for all page types
2. `curl http://localhost:3000/robots.txt` — shows rules for ChatGPT-User, PerplexityBot, Googlebot
3. Inspect page source on homepage — confirm `<meta property="og:image">` appears (from layout-level OG)
</verification>

<success_criteria>
- sitemap.ts and robots.ts created and TypeScript-clean
- metadataBase: new URL('https://stellamattina.com') present in layout.tsx metadata
- defaultOgImage exported from stella-mattina/src/lib/seo/og.ts
- No TypeScript errors introduced
- /sitemap.xml serves all page URL categories
- /robots.txt allows all major AI crawlers
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-hardening/04-01-SUMMARY.md`
</output>
