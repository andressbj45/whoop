---
phase: 01-foundation
plan: "05"
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified: []
autonomous: false
requirements:
  - DESIGN-02

must_haves:
  truths:
    - "The site renders correctly on a 375px mobile screen with no horizontal scroll and no clipped content"
    - "SM brand colors are visible in the rendered shell — not default Tailwind styles"
    - "The 'Book Now' CTA button is visible and clickable on both mobile and desktop"
    - "Plausible Analytics script is present in page source — no Google Analytics or cookie consent prompt"
  artifacts:
    - path: "stella-mattina/src/app/page.tsx"
      provides: "Shell homepage that renders correctly at 375px"
  key_links:
    - from: "browser at 375px viewport"
      to: "stella-mattina/src/components/layout/Header.tsx"
      via: "Mobile hamburger menu visible, no horizontal overflow"
      pattern: "hidden md:flex"
---

<objective>
Human verification that the Phase 1 foundation is correctly implemented before proceeding to Phase 2 content pages.

Purpose: Phase 2 builds dozens of content pages on top of this foundation. Catching layout, branding, or analytics issues now is far cheaper than fixing them across all Phase 2 pages later. This checkpoint verifies all five Phase 1 success criteria are visibly true.

Output: Human confirmation that the dev server shows a correctly branded, mobile-responsive SM shell with Plausible analytics.
</objective>

<execution_context>
@/Users/andressantos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andressantos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run automated pre-flight checks</name>
  <files></files>
  <action>
Run the following automated checks before the human verification:

```bash
cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina

# 1. Clean build (catches TypeScript errors)
npm run build 2>&1 | tail -20

# 2. Check Plausible is present and GA4 is absent in the built output
grep -r "plausible.io" .next/server/ 2>/dev/null | head -3
grep -r "google-analytics\|gtag\|connect.facebook" .next/server/ 2>/dev/null && echo "FAIL: Prohibited tracker found" || echo "PASS: No prohibited trackers"

# 3. Check SM tokens compiled into CSS
grep -r "sm-navy\|1C348C" .next/static/css/ 2>/dev/null | head -3

# 4. Verify content modules return correct counts
npx tsx -e "
import { getPractitioners } from './src/lib/content/practitioners'
import { getLocations } from './src/lib/content/locations'
import { getServices } from './src/lib/content/services'
import { getBlogPosts } from './src/lib/content/blog'
import { getContactInfo } from './src/lib/content/contact'
const results = {
  practitioners: getPractitioners().length,
  locations: getLocations().length,
  services: getServices().length,
  blogPosts: getBlogPosts().length,
  phones: getContactInfo().phones,
}
console.log(JSON.stringify(results, null, 2))
const pass = results.practitioners === 29 && results.locations === 7 && results.services === 11 && results.blogPosts === 61 && results.phones.length === 2
console.log(pass ? 'PASS: All content counts correct' : 'FAIL: Content count mismatch')
"

# 5. Start dev server (leave running for human verification)
npm run dev
```

Leave the dev server running for the human verification step below.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && npm run build 2>&1 | grep -E "error|✓ Compiled" | tail -5</automated>
    <manual>All automated checks pass before proceeding to human verification</manual>
  </verify>
  <done>
    - `npm run build` exits 0
    - No prohibited trackers (GA4, Meta Pixel) found in build output
    - Content layer returns correct record counts for all 5 data sources
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of SM-branded shell on desktop and mobile</name>
  <files></files>
  <action>
Pause for human to visually verify the running dev server at http://localhost:3000 against the checklist in how-to-verify below. No code changes in this task — purely verification.
  </action>
  <verify>
    <automated>echo "Human verification required — see how-to-verify checklist"</automated>
    <manual>All 6 verification checks below must pass before typing "approved"</manual>
  </verify>
  <done>Human types "approved" or describes issues to fix</done>
  <what-built>
Complete Phase 1 foundation:
- Next.js 16 project scaffolded with TypeScript, Tailwind v4, shadcn/ui
- SM brand tokens (navy, blue, warm palette + Roboto/Dynalight fonts) in globals.css
- Plausible Analytics in root layout (no GA4, no cookies)
- Content layer reading all 5 JSON files with TypeScript types
- Header, Footer, PageWrapper with SM logo and Book Now CTA
- Shell homepage at http://localhost:3000
  </what-built>
  <how-to-verify>
1. Open http://localhost:3000 in your browser

2. VISUAL BRAND CHECK (desktop):
   - Header: SM logo visible (not broken image), navy background or white header with navy text
   - "Book Now" button: visible in header with SM navy color (not default blue/gray)
   - Footer: SM logo visible, real phone numbers shown (214-942-3100 and 469-399-0355)
   - Page uses Roboto body font — NOT default system sans-serif

3. MOBILE RESPONSIVE CHECK (375px):
   - Open DevTools → Toggle Device Toolbar → set to 375px width (iPhone SE)
   - Confirm: NO horizontal scrollbar at 375px
   - Confirm: Hamburger menu icon visible in header (desktop nav is hidden)
   - Tap the hamburger: Sheet drawer opens with nav links and Book Now button

4. ANALYTICS CHECK:
   - Open DevTools → Network tab → filter by "plausible"
   - Reload the page
   - Confirm: Request to plausible.io/js/script.js (or similar) appears
   - Confirm: NO requests to google-analytics.com, googletagmanager.com, or connect.facebook.net

5. BOOK NOW CTA CHECK:
   - Click the "Book Now" button
   - Confirm: Opens https://mycw160.ecwcloud.com/portal22103/... in a new tab (target="_blank")

6. COLOR CHECK:
   - Header or nav area shows SM navy (#1C348C) — a dark blue-navy, not a generic blue
   - Page background is white, NOT the warm off-white (#FFFCE3) — that's for section backgrounds
  </how-to-verify>
  <resume-signal>
Type "approved" if all checks pass.
Or describe any issues found (e.g., "logo is broken", "colors look wrong", "mobile scrolls horizontally") and the executor will fix them before proceeding to Phase 2.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification is the success gate for this plan. All automated checks from Task 1 must pass AND human approval is required.
</verification>

<success_criteria>
- `npm run build` exits 0 (automated)
- No GA4 or Meta Pixel in build output (automated)
- Content layer: 29 practitioners, 7 locations, 11 services, 61 posts, 2 phones (automated)
- SM logo visible in header and footer (human)
- SM brand colors visible — not default Tailwind (human)
- Mobile 375px: no horizontal scroll, hamburger menu works (human)
- Plausible loads in Network tab; no Google Analytics (human)
- Book Now button opens patient portal in new tab (human)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
