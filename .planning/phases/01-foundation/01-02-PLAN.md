---
phase: 01-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - stella-mattina/src/app/globals.css
  - stella-mattina/src/app/layout.tsx
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - DESIGN-02

must_haves:
  truths:
    - "SM brand colors (navy #1C348C, blue #617CDF) are available as Tailwind utility classes (bg-sm-navy, text-sm-blue, etc.)"
    - "Roboto and Dynalight fonts load via next/font/google — no external <link> tags"
    - "Plausible Analytics script loads on every page — no Google Analytics or Meta Pixel present"
    - "globals.css extends the shadcn theme with SM tokens without breaking shadcn component styles"
  artifacts:
    - path: "stella-mattina/src/app/globals.css"
      provides: "Tailwind v4 @theme block with SM brand tokens and shadcn base variables"
      contains: "--color-sm-navy"
    - path: "stella-mattina/src/app/layout.tsx"
      provides: "Root layout with Roboto + Dynalight fonts via next/font/google and PlausibleProvider"
      contains: "PlausibleProvider"
  key_links:
    - from: "stella-mattina/src/app/globals.css"
      to: "Tailwind utility classes"
      via: "@theme { --color-sm-navy: #1C348C } → class bg-sm-navy"
      pattern: "--color-sm-navy.*#1C348C"
    - from: "stella-mattina/src/app/layout.tsx"
      to: "Plausible Analytics"
      via: "PlausibleProvider domain=\"stellamattina.com\" in <head>"
      pattern: "PlausibleProvider.*stellamattina.com"
    - from: "stella-mattina/src/app/layout.tsx"
      to: "next/font/google"
      via: "Roboto and Dynalight variables injected on <html> element"
      pattern: "roboto.variable.*dynalight.variable"
---

<objective>
Apply SM brand design tokens to Tailwind v4 and wire Plausible HIPAA-safe analytics into the root layout.

Purpose: Every component built in Plans 03 and 04 needs SM brand colors and fonts to be available as Tailwind utility classes. Analytics must be in root layout from day one — adding it later risks it being omitted from some pages.

Output: `globals.css` with SM brand tokens in `@theme {}`, root `layout.tsx` with Roboto/Dynalight fonts and `PlausibleProvider`.
</objective>

<execution_context>
@/Users/andressantos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andressantos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SM brand tokens to globals.css</name>
  <files>
    stella-mattina/src/app/globals.css
  </files>
  <action>
Open `stella-mattina/src/app/globals.css`. It currently contains the shadcn/ui baseline generated by `shadcn init` (with `@layer base { :root { ... } }` and CSS variable definitions).

DO NOT replace the shadcn base variables — they control shadcn component colors. Instead, MERGE SM brand tokens on top.

The final globals.css structure must be:

```css
@import "tailwindcss";
@import "@tailwindcss/typography";

/* shadcn/ui base variables (generated by shadcn init — do not remove) */
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 0 0% 3.9%;
    --card: 0 0% 100%;
    --card-foreground: 0 0% 3.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 0 0% 3.9%;
    --primary: 221 68% 33%;
    --primary-foreground: 0 0% 98%;
    --secondary: 224 62% 62%;
    --secondary-foreground: 0 0% 98%;
    --muted: 0 0% 93%;
    --muted-foreground: 0 0% 57%;
    --accent: 0 0% 96.1%;
    --accent-foreground: 0 0% 9%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 89.8%;
    --input: 0 0% 89.8%;
    --ring: 221 68% 33%;
    --radius: 0.5rem;
  }
  .dark {
    --background: 0 0% 3.9%;
    --foreground: 0 0% 98%;
    --card: 0 0% 3.9%;
    --card-foreground: 0 0% 98%;
    --popover: 0 0% 3.9%;
    --popover-foreground: 0 0% 98%;
    --primary: 224 62% 62%;
    --primary-foreground: 0 0% 9%;
    --secondary: 0 0% 14.9%;
    --secondary-foreground: 0 0% 98%;
    --muted: 0 0% 14.9%;
    --muted-foreground: 0 0% 63.9%;
    --accent: 0 0% 14.9%;
    --accent-foreground: 0 0% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 14.9%;
    --input: 0 0% 14.9%;
    --ring: 224 62% 62%;
  }
}

/* SM Brand Tokens — extend shadcn theme with Stella Mattina brand */
@theme {
  /* Brand colors — extracted from cam-theme/style.css */
  --color-sm-navy: #1C348C;
  --color-sm-blue: #617CDF;
  --color-sm-blue-hover: #4D63C7;
  --color-sm-blue-light: #9EB1F6;
  --color-sm-blue-card: #D6DEF9;
  --color-sm-warm: #FFFCE3;
  --color-sm-gray: #EDEDED;

  /* Typography — fonts loaded via next/font/google in layout.tsx */
  --font-sans: var(--font-roboto), system-ui, sans-serif;
  --font-display: var(--font-dynalight), Georgia, serif;

  /* Spacing (Ask Tia aesthetic: generous whitespace) */
  --spacing-section: 5rem;
  --spacing-section-lg: 8rem;
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

NOTE: If shadcn init generated a different set of CSS variables, preserve whatever shadcn generated — only add the SM @theme block and the @import "@tailwindcss/typography" line. Do not delete any shadcn-generated variables.

PITFALL TO AVOID: Do not use `tailwind.config.ts` or `tailwind.config.js` for SM brand colors. Tailwind v4 uses CSS-first configuration. Colors defined under `@theme { }` in globals.css automatically become utility classes (bg-sm-navy, text-sm-blue, etc.).
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && grep -E "--color-sm-navy|--color-sm-blue|--font-sans|@import.*typography" src/app/globals.css && npm run build 2>&1 | tail -5</automated>
    <manual>SM tokens present in globals.css and build passes</manual>
  </verify>
  <done>
    - globals.css contains @import "@tailwindcss/typography"
    - globals.css contains @theme block with --color-sm-navy: #1C348C
    - globals.css contains --color-sm-blue: #617CDF and all other SM tokens
    - globals.css contains --font-sans and --font-display referencing CSS variables
    - shadcn @layer base variables are preserved (not deleted)
    - `npm run build` passes without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure root layout with fonts and Plausible analytics</name>
  <files>
    stella-mattina/src/app/layout.tsx
  </files>
  <action>
Replace the contents of `stella-mattina/src/app/layout.tsx` with the following implementation. This adds Roboto + Dynalight via next/font/google and wraps the layout with PlausibleProvider.

```typescript
import type { Metadata } from 'next'
import { Roboto, Dynalight } from 'next/font/google'
import PlausibleProvider from 'next-plausible'
import './globals.css'

const roboto = Roboto({
  subsets: ['latin'],
  weight: ['400', '500', '700'],
  variable: '--font-roboto',
  display: 'swap',
})

const dynalight = Dynalight({
  subsets: ['latin'],
  weight: ['400'],
  variable: '--font-dynalight',
  display: 'swap',
})

export const metadata: Metadata = {
  title: {
    template: '%s | Stella Mattina',
    default: "Stella Mattina | Women's Health & Primary Care | OBGYN Dallas",
  },
  description:
    "Expert women's health and primary care in Dallas. Board-certified OBGYNs and Family Medicine doctors across 7 DFW locations.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${roboto.variable} ${dynalight.variable}`}>
      <head>
        <PlausibleProvider domain="stellamattina.com" />
      </head>
      <body className="font-sans antialiased">
        {children}
      </body>
    </html>
  )
}
```

CRITICAL RULES:
1. Do NOT add `'use client'` to layout.tsx — this would break the Next.js Metadata API and prevent server-side head tag rendering.
2. PlausibleProvider MUST be in `<head>` — not in `<body>`. Placing it in body causes the Plausible script to load after page content.
3. Use `next/font/google` for both fonts — do NOT use `<link>` tags for Google Fonts (causes layout shift and external DNS lookup).
4. NEVER add any GA4 (`gtag`, `google-analytics.com`), Meta Pixel (`connect.facebook.net`), or any other behavioral tracking script — this is a HIPAA-risk site.
5. Both font variables (`--font-roboto`, `--font-dynalight`) are referenced in globals.css `@theme` as `--font-sans` and `--font-display` respectively — the variable names must match exactly.
  </action>
  <verify>
    <automated>cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina && grep -E "PlausibleProvider|roboto.variable|dynalight.variable|font-sans" src/app/layout.tsx && npm run build 2>&1 | tail -5</automated>
    <manual>Confirm no 'use client' directive in layout.tsx and no Google Analytics references anywhere in the codebase</manual>
  </verify>
  <done>
    - layout.tsx imports and uses Roboto and Dynalight from next/font/google
    - layout.tsx has PlausibleProvider with domain="stellamattina.com" in <head>
    - layout.tsx has NO 'use client' directive
    - layout.tsx has NO gtag, GA4, or Meta Pixel references
    - Both font CSS variables (--font-roboto, --font-dynalight) are injected on <html> element
    - `npm run build` passes without TypeScript errors
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/andressantos/Desktop/work_in_progress/Woop/stella-mattina
# Verify SM tokens present
grep --color=always "sm-navy\|sm-blue\|PlausibleProvider\|font-roboto" src/app/globals.css src/app/layout.tsx
# Verify no GA4 anywhere
grep -r "google-analytics\|gtag\|facebook.net" src/ && echo "FAIL: Tracking found" || echo "PASS: No prohibited tracking"
# Final build check
npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
- globals.css: @theme block contains all 7 SM color tokens, font variables, and spacing tokens
- globals.css: @import "@tailwindcss/typography" present
- layout.tsx: Roboto and Dynalight loaded via next/font/google
- layout.tsx: PlausibleProvider wraps entire app, domain="stellamattina.com"
- layout.tsx: No 'use client', no GA4, no Meta Pixel
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
